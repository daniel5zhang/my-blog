# 本地私有内容隔离规则

## `src/content/diary/` 的边界
- `src/content/diary/` 属于**本地私有草稿 / 随记区**。
- 该目录**不进入 Daniel实验室 正式公开站点基线**。
- 该目录**不进入运营资产**。
- 该目录**不进入 OpenClaw 产品化资产**。
- 该目录**不接入发布流**，也不作为飞书双留存对象。

## 处理原则
- 保留本地文件，不做删除。
- 通过 Git 忽略规则让其不再出现在 `git status` 中。
- 若后续其中某条内容需要升级为正式资产，应先迁移到正式内容目录，再单独审计和提交。
