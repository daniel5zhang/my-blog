---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="历史兼容说明" description="Daniel实验室 已不再把项目作为前台独立内容类型展示">
  <section class="compat-page">
    <p class="kicker">历史兼容路由</p>
    <h1>项目栏目已收口</h1>
    <p class="lead">
      Daniel实验室 现在是一套文章型内容中枢，前台不再把“项目”作为一级内容结构单独展示。
    </p>
    <div class="compat-card">
      <p>
        这一路由当前保留，只用于历史兼容，避免旧链接立刻失效。后续内容会优先归入文章与专题体系。
      </p>
      <div class="actions">
        <a href="/articles" class="btn btn-primary">去文章页</a>
        <a href="/topics" class="btn">看专题入口</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .compat-page {
    max-width: 760px;
    margin: 0 auto;
    padding: 2rem 0;
  }

  .kicker {
    color: var(--accent-color);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .lead {
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 1.5rem;
  }

  .compat-card {
    padding: 1.5rem;
    border: 1px solid var(--border-color);
    border-radius: 14px;
    background: var(--bg-secondary);
  }

  .compat-card p {
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
</style>
